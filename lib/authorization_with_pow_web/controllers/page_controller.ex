defmodule AuthorizationWithPowWeb.PageController do
  use AuthorizationWithPowWeb, :controller

### def index(conn, _params) do
###    if Pow.Plug.current_user(conn) do
###     conn
###      |> redirect(to: "/")
###   else
###      render conn, :index
###    end
###  end
end
